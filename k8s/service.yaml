apiVersion: v1
kind: Service
metadata:
  name: contextforge-svc
  namespace: mcp-farm
  labels:
    app: contextforge
    project: mcp-farm
spec:
  type: ClusterIP
  selector:
    app: contextforge
  ports:
    - name: http
      port: 4444
      targetPort: 4444
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: contextforge-svc
  namespace: mcp-farm
  labels:
    app: contextforge
    project: mcp-farm
spec:
  type: LoadBalancer
  selector:
    app: contextforge
  ports:
    - name: http
      port: 80
      targetPort: 4444
      protocol: TCP
